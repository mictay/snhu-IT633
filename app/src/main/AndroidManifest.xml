<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mictay.snhu.it633.acerestaurantapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ace_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ace_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AceRestaurantApp">
        <activity
            android:name=".view.home.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.AceRestaurantApp.NoActionBar"></activity>
        <activity
            android:name=".view.MainActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>